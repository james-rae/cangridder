import fs from 'fs';
import { GridCenters, Line, Point } from './grid';

const parseTrend = async (path: string): Promise<Map<string, number>> => {
    const mappy = new Map<string, number>();

    // read CSV

    return mappy;
};
